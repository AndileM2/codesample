export interface Message {
  message: string;
}

export interface Version {
  message: string;
  version: number;
}

export interface UserData{
  name: string;
  age: number;
  email: string;
  balance: number;
}